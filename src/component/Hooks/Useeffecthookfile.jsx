import React from 'react'

const Useeffecthookfile = (props) => {
  return (
    <>
    <div>userid:{props.userid}</div>
    <div>id:{props.id}</div>
    <div>title:{props.title}</div>
    <div>body:{props.body}</div>

    </>
  )
}

export default Useeffecthookfile